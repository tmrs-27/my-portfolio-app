<div class="min-h-screen">
  <%# ヒーローセクション %>
  <section class="relative overflow-hidden bg-gradient-to-br from-blue-50 via-white to-slate-50 dark:from-gray-900 dark:via-gray-900 dark:to-slate-900">
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top_right,_var(--tw-gradient-stops))] from-blue-100/40 to-transparent dark:from-blue-900/10 dark:to-transparent"></div>
    <%# 浮遊する装飾オーブ %>
    <div class="absolute right-[10%] top-[20%] h-72 w-72 rounded-full bg-blue-200/30 dark:bg-blue-500/10 blur-3xl animate-float"></div>
    <div class="absolute right-[30%] top-[60%] h-96 w-96 rounded-full bg-sky-200/20 dark:bg-sky-500/5 blur-3xl animate-float-slow" style="animation-delay: -2s"></div>
    <div class="absolute left-[5%] top-[40%] h-64 w-64 rounded-full bg-indigo-200/25 dark:bg-indigo-500/10 blur-3xl animate-float-slow" style="animation-delay: -4s"></div>
    <div class="container relative z-10 mx-auto px-4 py-20 md:py-28 lg:py-36">
      <div class="max-w-3xl">
        <p class="mb-4 inline-block rounded-full bg-blue-100 dark:bg-blue-900/30 px-4 py-1.5 text-sm font-medium text-blue-600 dark:text-blue-400 animate-fade-up">
          My Rails Portfolio
        </p>
        <h1 class="text-4xl font-extrabold tracking-tight text-gray-900 dark:text-white sm:text-5xl lg:text-6xl animate-fade-up" style="animation-delay: 0.1s">
          <span class="block">アイデアを</span>
          <span class="block bg-gradient-to-r from-blue-600 to-blue-500 bg-clip-text text-transparent animate-pulse-soft">形にしよう</span>
        </h1>
        <p class="mt-6 max-w-2xl text-lg text-gray-600 dark:text-gray-400 animate-fade-up" style="animation-delay: 0.2s">
          プログラミングの学びやプロジェクトの記録を、シンプルに蓄積・共有できるブログです。
        </p>
        <div class="mt-10 flex flex-wrap gap-4 animate-fade-up" style="animation-delay: 0.3s">
          <%= link_to posts_path, class: "inline-flex items-center gap-2 rounded-lg bg-blue-600 px-6 py-3 text-base font-semibold text-white shadow-lg shadow-blue-600/25 transition-all duration-200 hover:bg-blue-700 hover:shadow-xl hover:shadow-blue-600/30 hover:-translate-y-0.5" do %>
            投稿を見る
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          <% end %>
          <%= link_to new_post_path, class: "inline-flex items-center gap-2 rounded-lg border-2 border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 px-6 py-3 text-base font-semibold text-gray-700 dark:text-gray-300 transition-all duration-200 hover:border-blue-500 hover:text-blue-600 dark:hover:text-blue-400" do %>
            新規投稿
          <% end %>
        </div>
      </div>
    </div>
  </section>

  <%# 最近の投稿 %>
  <section class="border-t border-gray-100 dark:border-gray-800 bg-white dark:bg-gray-900/50 py-16 md:py-24">
    <div class="container mx-auto px-4">
      <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-6 mb-12">
        <div>
          <h2 class="text-2xl font-bold text-gray-900 dark:text-white sm:text-3xl">最近の投稿</h2>
          <p class="mt-2 text-gray-600 dark:text-gray-400">最新の記事をお届けします</p>
        </div>
        <%= link_to posts_path, class: "text-blue-600 dark:text-blue-400 font-semibold hover:underline inline-flex items-center gap-1 shrink-0" do %>
          すべて見る
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
          </svg>
        <% end %>
      </div>

      <% if @recent_posts.any? %>
        <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
          <% @recent_posts.each_with_index do |post, index| %>
            <%= link_to post, class: "group block animate-fade-up", style: "animation-delay: #{index * 0.05}s" do %>
              <article class="h-full overflow-hidden rounded-2xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 shadow-sm transition-all duration-300 group-hover:shadow-xl group-hover:border-blue-200 dark:group-hover:border-blue-800/50">
                <div class="p-6">
                  <% if post.categories.any? %>
                    <div class="mb-3 flex flex-wrap gap-2">
                      <% post.categories.first(2).each do |category| %>
                        <span class="inline-block px-2.5 py-0.5 rounded-full text-xs font-medium <%= category.badge_classes %>">
                          <%= category.name %>
                        </span>
                      <% end %>
                    </div>
                  <% end %>
                  <h3 class="text-lg font-bold text-gray-900 dark:text-white line-clamp-2 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
                    <%= post.title %>
                  </h3>
                  <p class="mt-2 text-sm text-gray-500 dark:text-gray-400 line-clamp-3">
                    <%= post.body %>
                  </p>
                  <p class="mt-4 text-xs text-gray-400 dark:text-gray-500">
                    <%= post.created_at.strftime("%Y年%m月%d日") %>
                  </p>
                </div>
              </article>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <div class="rounded-2xl border border-dashed border-gray-300 dark:border-gray-600 bg-gray-50 dark:bg-gray-800/50 py-16 text-center">
          <p class="text-gray-500 dark:text-gray-400 mb-6">まだ投稿がありません</p>
          <%= link_to new_post_path, class: "inline-flex items-center gap-2 rounded-lg bg-blue-600 px-5 py-2.5 text-sm font-semibold text-white transition-colors hover:bg-blue-700" do %>
            最初の投稿を作成
          <% end %>
        </div>
      <% end %>
    </div>
  </section>

  <%# CTA セクション %>
  <section class="relative overflow-hidden border-t border-gray-100 dark:border-gray-800 bg-gradient-to-br from-blue-600 to-blue-700 py-16 md:py-20">
    <div class="absolute -right-20 -top-20 h-64 w-64 rounded-full bg-white/10 animate-float"></div>
    <div class="absolute -left-20 -bottom-20 h-48 w-48 rounded-full bg-white/5 animate-float-slow" style="animation-delay: -3s"></div>
    <div class="container relative z-10 mx-auto px-4 text-center">
      <h2 class="text-2xl font-bold text-white sm:text-3xl">さあ、はじめましょう</h2>
      <p class="mx-auto mt-4 max-w-xl text-blue-100">
        あなたの考えを記録し、共有する。シンプルで美しいブログ体験を。
      </p>
      <%= link_to new_post_path, class: "mt-8 inline-flex items-center gap-2 rounded-lg bg-white px-6 py-3 text-base font-semibold text-blue-600 shadow-lg transition-all duration-200 hover:bg-blue-50 hover:shadow-xl hover:-translate-y-0.5" do %>
        新規投稿を作成
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
        </svg>
      <% end %>
    </div>
  </section>
</div>
