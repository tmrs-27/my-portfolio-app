<div class="min-h-screen transition-colors duration-500 bg-white dark:bg-gray-900">
  <div class="container mx-auto py-10 px-4">
    <div class="flex justify-between items-center mb-8 animate-fade-up">
      <h1 class="text-4xl font-extrabold text-gray-700 dark:text-gray-300">カテゴリ一覧</h1>
      <%= link_to "新規カテゴリ", new_category_path, class: "bg-blue-600 hover:bg-blue-700 hover:scale-105 active:scale-95 text-white font-bold py-2 px-6 rounded-lg shadow-md transition-all duration-200" %>
    </div>

    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 overflow-hidden animate-fade-up" style="animation-delay: 0.1s">
      <ul class="divide-y divide-gray-100 dark:divide-gray-700">
        <% @categories.each_with_index do |category, index| %>
          <li class="flex items-center justify-between px-6 py-4 hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-all duration-200 hover:pl-8">
            <div class="flex items-center gap-3">
              <%= link_to category, class: "inline-block px-3 py-0.5 rounded-full text-sm font-medium #{category.badge_classes} transition-all duration-200 hover:scale-105 hover:shadow-md" do %>
                <%= category.name %>
              <% end %>
              <span class="text-sm text-gray-500 dark:text-gray-400">
                <%= category.posts.count %>件の投稿
              </span>
            </div>
            <div class="flex gap-2">
              <%= link_to "編集", edit_category_path(category), class: "text-sm text-blue-500 hover:text-blue-600 font-medium" %>
              <%= button_to "削除", category, method: :delete, form: { data: { turbo_confirm: "このカテゴリを削除しますか？投稿からは外されます。" } }, class: "text-sm text-red-500 hover:text-red-600 font-medium" %>
            </div>
          </li>
        <% end %>
      </ul>

      <% if @categories.empty? %>
        <div class="px-6 py-12 text-center text-gray-500 dark:text-gray-400">
          <p>カテゴリがまだありません。</p>
          <%= link_to "最初のカテゴリを作成", new_category_path, class: "mt-4 inline-block text-blue-500 hover:text-blue-600 font-medium" %>
        </div>
      <% end %>
    </div>

    <div class="mt-6">
      <%= link_to "← 投稿一覧に戻る", posts_path, class: "text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 font-medium" %>
    </div>
  </div>
</div>
